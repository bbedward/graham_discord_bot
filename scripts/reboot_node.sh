#!/bin/bash

# Uncomment if you want the bot to attempt to restart the node when it thinks something is wrong

#container_id=$(docker ps --filter="name=nifty_austin" -q)
#curl -g -d '{"action":"stop"}' '[::1]:7076'
#sleep 15
#docker restart $container_id
